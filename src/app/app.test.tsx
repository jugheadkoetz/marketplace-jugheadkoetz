 import { describe, it } from "node:test";
import { spec } from "node:test/reporters";
const sum = (x: number, y:number) =>{
    return x + y
}    
describe("app component",() => {
     it('should sum correctily',() => {
     });
});

export default {};